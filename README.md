# Simpfunk

```
+.: sf v0.0.1 :.+
Simpfunk is an esoteric programming language that prints out strings.

Usage: sf --file <program.sf>
       - Execute program.
Usage: sf --dump <string> [-1]
       - Translate a string to a simpfunk program.
         * -1: print characters one by one.
```
